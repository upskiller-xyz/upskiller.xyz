# Copy to .env.local for local development (gitignored).
# Baked into the JS bundle at build time — see src/utils/AssetPathManager.ts.

# Base URL for images, icons, dynamic JSON, and legal docs.
# Defaults to the Scaleway bucket if unset.
VITE_ASSET_BASE_URL=
